select()
  display block
  box-sizing border-box
  width 100%
  height 40px
  outline 0
  border solid transparent 10px
  border-radius 4px
  background rgba(0, 0, 0, .1)
  color $black
  font-weight 500
  font-size inherit
  font-family inherit
  line-height inherit
  transition .3s ease
